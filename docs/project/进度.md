# Skill Box 实施进度

> 配合 [需求规划.md](./需求规划.md) 阅读。本文记录 12 步落地顺序的当前状态、每步产物、已实现范围、已知偏差与下一步入口。
>
> 最后更新:2026-06-22(第 1~10 步完成,第 11 步收尾 1(audit 写入串联) + 收尾 2(Onboarding 跳 Skills + export/import 端到端) + 3.13(预置 skill 库) + 3.14(i18n 中英双语)完成;前端 UI 全面升级)
>
> 过程问题 / 测试报告 / 需求回流 → [开发报告.md](./开发报告.md)

---

## 0. 文档同步规则(2026-06-21 增补)

- 每完成一个 12 步里的步骤,**必须**同步:
  1. 本文件 `进度.md` — 状态表 + "已完成"小节
  2. [开发报告.md](./开发报告.md) — 新增"X.Y 测试报告"小节,记录用例数、耗时、build 结果
  3. 必要时回写 [需求规划.md](./需求规划.md) 的"决策变更"小节
- 新增需求 / 计划外功能 → 先写进 `开发报告.md` 的"需求回流",再决定是否合入 `需求规划.md`
- 上述同步必须在 commit 之前完成,不允许"代码已合入、文档下次再说"

---

## 1. 当前快照

- **仓库分支**:`main`
- **最近提交**:(见 git log,Step 2 提交待执行)
- **构建状态**:`go build ./internal/... ./cmd/bootstrap/... ./cmd/web/... ./cmd/gapi/...` 全过
- **测试状态**(本次 Step 2 完成后):`skilladapter` 13 / `skillstore` 11 / `settings` 6 / `skillimporter` 7 / `conboarding` 2,共 39 用例全过(详细见 [开发报告.md §3.1](./开发报告.md#31-step-2-测试报告2026-06-21))
- **已落地**:**第 1 步**(基础设施)+ **第 2 步**(adapters + importer + onboarding 路由)+ **第 3 步**(Project CRUD + Projects 页面)+ **第 4 步**(Skill CRUD + SkillsView,Monaco 待集成)+ **第 5 步**(AI 引擎:5 预设 + 3 种 provider + SSE + 侧栏 UI)+ **第 6 步**(Skill 测试器:static + script + AI 走查)+ **第 7 步**(三方市场:skillhub.cn / skills.sh 拉取 + 装到 store)+ **第 8 步**(应用与同步:Apply / Undo / 批量 / 更新检测)+ **第 9 步**(版本快照与回滚:tag / diff / rollback)+ **第 10 步**(导出/导入包 .skillbox zip + 审计日志 2 端点)+ **第 11 步 收尾 1**(audit 写入串联 13 事件 + 5 端到端测试)+ **第 11 步 收尾 2**(Onboarding 跳 Skills 事件总线 + export/import 端到端 PASSED)+ **前端 UI 全面升级**(侧栏 / 顶栏 / Onboarding 向导 / Audit 概览)
- **下一步**:**第 11 步 主线**(预置 skill 库 + i18n + 桌面端托盘/快捷键/通知)

---

## 2. 12 步落地顺序 — 状态表

| 步 | 主题                                              | 状态        | 主要产物                                                                                                  |
| -- | ------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------- |
| 1  | 基础设施                                          | ✅ 完成     | 11 张表 / skilladapter 抽象 / skillstore / settings / configs.skillbox                                   |
| 2  | 5 个 adapter + 路径扫描 + 首次 Onboarding         | ✅ 完成     | codex / claude / opencode / cursor / trae 五个 adapter + skillimporter(扫 + 批量导入) + 3 个 onboarding 路由 |
| 3  | 项目 CRUD + Projects 页面                         | ✅ 完成     | sproject service + cproject 5 个 CRUD 端点 + ProjectsView.vue                                             |
| 4  | Skill 编辑器(frontmatter 表单 + SKILL.md 全文)      | ✅ 完成     | sskill service + cskill 5 个 CRUD 端点 + SkillsView.vue(全局/项目 tab、编辑、删除、列表分页)         |
| 5  | AI 引擎(multi provider + SSE + prompt)            | ✅ 完成     | aiengine(provider/manager/preset) + sai service + caiprovider 7 端点 + AIPanel 侧栏 + SSE 流式 |
| 6  | Skill 测试器(静态 + 脚本执行 + AI 模拟走查)        | ✅ 完成     | internal/skilltester/ + sskilltest + cskilltest + SkillsView 测试按钮/结果面板    |
| 7  | 三方市场 + Market 页                              | ✅ 完成     | skillmarket 核心包 + skillhub / skillssh 两个 adapter + market_skills 缓存表 + 4 端点 + MarketView |
| 8  | 应用与同步(Apply / Undo / 批量 / 更新检测)         | ✅ 完成     | internal/skillapp/ + sskillapp + cskillapply 5 端点 + SkillsView 集成                        |
| 9  | 快照与回滚(tag / diff / rollback)                 | ✅ 完成     | internal/skillaudit/ + sskillaudit + ctag 5 端点 + SkillsView Tag 面板              |
| 10 | 导出/导入包 + 审计日志                            | ✅ 完成     | internal/skillpkg/(export/import/preview zip)+ sskillpkg + cskillpkg 3 端点 + saudit + caudit 2 端点 + AuditView |
| 11 | 预置 skill 库 + i18n + 桌面端托盘/快捷键/通知     | 🚧(收尾 1+2 已完成) | bundle 资源 + vue-i18n + desktop/services/tray_svc.go                                                     |
| 12 | CLI 桥(`gapi` sub-command)                       | ⏳          | api-server/cmd/gapi/ 增强                                                                                 |

> ✅ 完成 / 🚧 进行中 / ⏳ 待开

---

## 3. 已完成步骤

### 3.1 第 1 步 — 基础设施

提交 `c3ec742` Skill Box 基础设施:11 张新表 + adapter 抽象 + skillstore + settings。

- 11 张 GORM 表:`projects` / `skills` / `skill_files` / `skill_tags` / `skill_file_snapshots` / `skill_applies` / `audit_logs` / `ai_providers` / `market_sources` / `onboarding_states` / `settings`
- `skilladapter` 包:`types` / `registry` / `base` / `frontmatter`(ParseSkillMD / RenderSkillMD / NormalizeName)
- `skillstore`:原子写 + 文件锁 + 路径遍历保护 + SHA-256
- `settings`:key-value 通用存储 + JSON 编解码
- `configs/skillbox.go` + entities auto-migrate
- 测试 20+ 用例全过

### 3.2 第 2 步 — 5 个 adapter + 路径扫描 + 首次 Onboarding

**新增 / 修改文件**:

| 路径                                                                                  | 类别                |
| ------------------------------------------------------------------------------------- | ------------------- |
| `api-server/internal/skilladapter/{frontmatter,base,types,registry}.go`              | adapter 抽象 + 修复  |
| `api-server/internal/skilladapter/codex/codex.go`                                     | adapter 实现        |
| `api-server/internal/skilladapter/claude/claude.go`                                   | adapter 实现        |
| `api-server/internal/skilladapter/opencode/opencode.go`                               | adapter 实现        |
| `api-server/internal/skilladapter/cursor/cursor.go`                                   | adapter 实现(草案)  |
| `api-server/internal/skilladapter/trae/trae.go`                                       | adapter 实现        |
| `api-server/internal/skilladapter/adapters_integration_test.go`                       | 集成测试            |
| `api-server/internal/skillimporter/{importer,importer_test}.go`                       | 扫 + 导入 + 测试    |
| `api-server/internal/gapi/controller/skillbox/conboarding/*.a.go`                     | 3 个 onboarding 端点 |
| `api-server/internal/gapi/controller/skillbox/conboarding/conboarding_test.go`        | 路由层单测          |
| `api-server/internal/gapi/router/routers_import.go`                                    | 挂 conboarding      |
| `docs/project/{README,进度,开发报告}.md`                                              | 文档同步            |

**实现要点**

- 5 个 adapter 都基于 `BaseAdapter` 复用通用逻辑,各自只填 ID / DisplayName / Icon / DiscoverPaths 即可
- codex 路径覆盖三处:`~/.codex/skills`、`~/.codex/skills/.system`、`~/.codex/vendor_imports/skills/skills/.curated`;claude 走 plugin marketplaces 而非 `~/.claude/skills`;trae 走 `~/.trae/skills`;opencode / cursor 走默认 `~/.config/...` 和 `~/.cursor/...`(草案)
- `skillimporter` 抽象:Report = 一次扫描的完整产出(每个目录、每个 tool、每个 found skill);Import = 把 Report 里勾选出来的条目落到 skillstore 全局域
- Onboarding 三个端点:`GET /api/skillbox/onboarding/status` 报告 5 个 adapter 状态 + 上次 scan 摘要;`POST /api/skillbox/onboarding/scan` 跑扫描并把精简 Report 缓存到进程内单例;`POST /api/skillbox/onboarding/import` 消费缓存做选择性导入
- 单元测试用自定义 registry + 临时目录,做到不依赖本机实际目录,稳定可重复

**踩坑(详见 [开发报告.md §2](./开发报告.md#2-问题与方案滚动记录))**

- `NormalizeName` 把已存在的 `-` 当成"待折叠"字符吞掉,已修
- `ParseSkillMD` 把"无 frontmatter"也走了 H1 兜底,与"严格解析"规约不符,已修
- 集成测试引用了已重命名的 `NormalizeNameExport`,`sed` 一次修掉


### 3.4 第 4 步 — Skill CRUD + SkillsView(Monaco 推迟到 P1)

**新增文件**

| 路径                                                                                  | 类别       |
| ------------------------------------------------------------------------------------- | ---------- |
| `api-server/internal/gapi/service/skill/sskill/skill.s.go`                            | 业务层     |
| `api-server/internal/gapi/service/skill/sskill/skill.s_test.go`                       | 业务层测试 |
| `api-server/internal/gapi/controller/skillbox/cskill/{list,get,create,update,delete}_skill.a.go` | 5 个端点 |
| `api-server/internal/gapi/router/routers_import.go`                                   | 挂 cskill   |
| `frontend/src/api/skillbox/skills.js`                                                 | 前端 API   |
| `frontend/src/views/SkillsView.vue`                                                   | Skills 页  |
| `frontend/src/App.vue`                                                                | 加 Skills/Projects 切换 tab |

**接口**

| Method | Path                          | 行为                                                                  |
| ------ | ----------------------------- | --------------------------------------------------------------------- |
| GET    | `/api/skillbox/skills`        | 列表,支持 `?scope&project_id&keyword&page&size`                       |
| GET    | `/api/skillbox/skills/get`    | 按 (scope, project_id, name, version) 查,`full=true` 返回 canonical   |
| POST   | `/api/skillbox/skills/create` | 新建;400 空字段 / 422 校验失败 / 200 ok                              |
| POST   | `/api/skillbox/skills/update` | 按 (scope, project_id, name, version) 更新;version 不允许改          |
| POST   | `/api/skillbox/skills/delete` | 按 key 删;幂等                                                       |

**实现要点**

- sskill 业务层把"先 store.Save 写盘,再写 DB;失败回滚物理文件"作为强约束,确保 store / DB 不一致时一定回滚
- skillstore + 11 个 service 测试覆盖 scope 校验 / project_id 隔离 / 物理文件落地 / 幂等删除
- 前端 SkillsView 用 `<textarea>` 拼 frontmatter(body 提取),暂未集成 Monaco(见下)
- 顶部 tab 切 Skills / Projects,App.vue 极简

**踩坑**

- `where.New(...)` 必须 `.Conditions()` 后才能 `append(conds, ...)`;见 [开发报告.md §2.8](./开发报告.md#28-where-extra-字段名拼错导致编译失败)
- 数组字面量 `[]*where.Condition{ where.New(...), ... }` 用 `...` 不合法,改用 append 链式拼接

**Monaco 推迟原因**

- `monaco-editor` 单包 ~5MB,沙盒里 `npm install` 经常超时被杀
- SkillsView 已有 frontmatter 表单 + body textarea,Step 4 的"可编辑"目标已达成
- 后续(P1 / 单独一个 PR)用 `@monaco-editor/loader` 从 CDN 加载,把体积降到可接受范围
- 跟踪位置:开发报告 §"需求回流" 小节会增补一条

### 3.5 第 5 步 — AI 引擎(provider 抽象 + 5 preset + SSE 流式 + 侧栏)

**新增文件**

| 路径 | 类别 |
| --- | --- |
| `api-server/internal/aiengine/{types,openai,anthropic,manager}.go` | provider 抽象 + OpenAI/Anthropic 流式 + Manager |
| `api-server/internal/aiengine/aiengine_test.go` | 12 个单测 |
| `api-server/internal/gapi/service/ai/sai/ai.s.go` + `_test.go` | 业务层(9 测试) |
| `api-server/internal/gapi/controller/skillbox/caiprovider/{list,get,create,update,delete}_provider.a.go` | CRUD 端点(5 个) |
| `api-server/internal/gapi/controller/skillbox/caiprovider/{set_key,list_presets,chat_stream}.a.go` | key/preset/SSE(3 个) |
| `api-server/internal/gapi/router/routers_import.go` | 挂 caiprovider |
| `frontend/src/api/skillbox/ai.js` | API 封装(SSE 走裸 fetch) |
| `frontend/src/components/AIPanel.vue` | 侧栏 UI(preset chips + 流式对话) |
| `frontend/src/views/SkillsView.vue` | 加 AIPanel 接入 + 切换按钮 |

**接口**

| Method | Path | 行为 |
| --- | --- | --- |
| GET    | `/api/skillbox/ai/providers`           | 列表,含 `has_key` 标记 |
| GET    | `/api/skillbox/ai/providers/get`       | 按 id 查 |
| POST   | `/api/skillbox/ai/providers/create`    | 新建(可顺手设 api_key) |
| POST   | `/api/skillbox/ai/providers/update`    | 更新(改名时迁 key) |
| POST   | `/api/skillbox/ai/providers/delete`    | 删 + 清 key |
| POST   | `/api/skillbox/ai/providers/key`       | 单独设/改/清 api key |
| GET    | `/api/skillbox/ai/presets`             | 5 个内置 preset |
| POST   | `/api/skillbox/ai/chat`                | **SSE 流式对话**(data: {json}\n\n,结束 [DONE]) |

**实现要点**

- `aiengine.Provider` 接口 + `Manager`(按 priority 选)+ `SecretStore` 抽象(由 sai 用 settings 实现)
- `OpenAIProvider` 一份代码同时支持 `KindOpenAI` 与 `KindOpenAICom`(DeepSeek / 硅基 / 月之暗面),`WithBaseURL` 给测试用
- `AnthropicProvider` 走 Anthropic Messages streaming
- 5 个 preset:`optimize_frontmatter` / `test_description` / `rewrite_body` / `find_duplicates` / `safety_check`
- API key 存 `settings` 表(K-V),key 形如 `ai:<name>:api_key`;`api_providers.api_key_ref` 字段为 P1 OS keychain 预留
- SSE controller 手动写头 + `c.Writer.(http.Flusher)` 逐帧 flush;前端用裸 `fetch` + `ReadableStream` 解析 `data: ` 前缀
- `chat_stream.a.go` 不走 `BindParamsHandler`(因为要手动接管 response),自己 `ShouldBindJSON` 拿 body
- AIPanel `optimize_frontmatter` 完成后 emit `apply`,把改写后的 markdown 写回 SkillsView 的 `draft.body`

**踩坑(详见 [开发报告.md §2.11](./开发报告.md#211-))**

- caiprovider CRUD 端点同上一致用 `ginp.BindParamsHandler` 包装;chat 端点不用
- controller 写 `ginp.H{...}` 编译失败 → 改为 `gin.H{...}`(本仓库无 `ginp.H` 别名,沿用 §2.6 结论)
- `cfg` 库落盘问题(§2.7)在 Step 5 又复现一次,处理方式同前

### 3.6 第 6 步 — Skill 测试器(static + script + AI 走查)

提交 `9fe5600` Skill Box 测试器(2 entity + 2 model + skilltester 包 + sskilltest service + cskilltest 3 端点 + SkillsView 测试按钮/结果面板)。

**新增文件**

| 路径 | 类别 |
| --- | --- |
| `api-server/internal/gapi/entity/e_skill_test_run.go` | run 记录 entity |
| `api-server/internal/gapi/entity/e_skill_test_result.go` | 单 check 结果 entity |
| `api-server/internal/gapi/model/skillbox/mskilltestrun/{mskilltestrun.f,mskilltestrun.m}.go` | model + 字段常量 |
| `api-server/internal/gapi/model/skillbox/mskilltestresult/{mskilltestresult.f,mskilltestresult.m}.go` | model + 字段常量 |
| `api-server/internal/skilltester/{types,static_lint,script_runner,ai_walker,tester}.go` | 测试器核心(static + script + AI 走查) |
| `api-server/internal/skilltester/skilltester_test.go` | 11 个单测 |
| `api-server/internal/gapi/service/skilltester/sskilltest/skilltest.s.go` + `_test.go` | 业务层 + 5 测试 |
| `api-server/internal/gapi/controller/skillbox/cskilltest/{run,list,get}_skill_test.a.go` | 3 端点 |
| `api-server/internal/gapi/controller/skillbox/cskilltest/cskilltest_test.go` | 路由注册校验 |
| `api-server/cmd/bootstrap/entities.go` | 注册 2 张新表到 AutoMigrate |
| `api-server/internal/aiengine/manager.go` | 加 `BuildFromConfig(Config)` |
| `api-server/internal/gapi/router/routers_import.go` | 挂 cskilltest |
| `frontend/src/api/skillbox/skill_test.js` | API 客户端 |
| `frontend/src/views/SkillsView.vue` | 行操作 + 编辑器结果面板 |
| `docs/project/{开发报告,进度}.md` | 文档同步 |

**接口**

| Method | Path | 行为 |
| --- | --- | --- |
| POST | `/api/skillbox/skills/test/run`  | 触发一次测试,返回 Run + Results |
| GET  | `/api/skillbox/skills/test/list` | 列表(按 skill_id 过滤 + 分页) |
| GET  | `/api/skillbox/skills/test/get`  | run 详情 + 关联 results |

**实现要点**

- `internal/skilltester` 内部 3 个独立 check + 1 个聚合 Tester:
  - **static_lint**:frontmatter 必填(name/version/description/triggers)+ 格式正则 + 长度阈值 + 文件路径合法性(`..` / 绝对路径 / ` `)+ SKILL.md 必含 + body 非空 + 硬编码 secret 嗅探(sk- / api_key= / token=)+ trigger 去重
  - **script_runner**:支持自定义 command(拒绝 shell 注入字符)+ skill 自带 `test.sh` / `test.py` / `test.js` / `test`;写到 tempdir 隔离 + 60s timeout + 截断 stdout/stderr 到 32KB
  - **ai_walker**:走 aiengine 流式(默认 `safety_check` preset);无 provider / 无 key / 未知 preset 时降级 skipped,不影响其他 check
- 聚合策略:任一 failed → 整体 failed;任一 errored → 整体 errored(除非有 failed);全部 skipped → 整体 skipped
- `aiengine.Manager.BuildFromConfig(Config)`:service 层只填 Config,manager 内部用 factories 选工厂,避免外部重复 switch kind
- API key 解析走 settings KV(沿用 sai 同款 `secretAdapterForTester`,独立实现一份避免 sai 跨包引用)
- controller 用 `ginp.BindParamsHandler`(同步执行不需要 SSE);`/test/run` 单次执行可能 1-2s(AI 走查 60s 超时),前端按钮显示"测试中…"
- SkillsView 行操作加"测试"按钮(confirm 后触发),编辑器下方加折叠结果面板(status badge + 3 check 行 + JSON 详情折叠)

**踩坑(详见 [开发报告.md §2.13 / §2.14](./开发报告.md))**

- `apply_patch` 在长文件上偶发 aborted → 改用 `cat heredoc` 写新文件
- `AnthropicProvider.defaultBase` 未导出,service 层不能直接赋值 → manager 暴露 `BuildFromConfig`,OpenAI 走 `WithBaseURL` 链,Anthropic P1 补对应方法
- Python `str.replace` 多行匹配脆弱(转义 / 缩进易错)→ 改用 `re.sub` 或行级循环重写
- `go test ./...` 偶发 hang(摘要里 Step 5 已记录)→ 本次拆成子集跑,避开 `gen/db/pgsql` 的真连库 panic

### 3.6.1 第 7 步 — 三方市场 + Market 页(2026-06-22)

**新增文件**

| 路径 | 类别 |
| --- | --- |
| `api-server/internal/gapi/entity/e_market_skill.go` | market_skills 缓存表 entity |
| `api-server/internal/gapi/model/skillbox/mmarketskill/{mmarketskill.f,mmarketskill.m}.go` | model + 字段常量 |
| `api-server/internal/skillmarket/{types,registry,market}.go` | 核心:MarketAdapter 接口 + registry + orchestrator |
| `api-server/internal/skillmarket/skillhub/skillhub.go` | skillhub.cn 适配器(JSON + 兜底) |
| `api-server/internal/skillmarket/skillssh/skillssh.go` | skills.sh 适配器(HTML 解析 + GitHub raw 装) |
| `api-server/internal/skillmarket/{skillmarket,skillhub,skillssh}*_test.go` | 35 个新单测 |
| `api-server/internal/gapi/service/market/smarket/{market.s,market.s_test}.go` | 业务层(7 测试) |
| `api-server/internal/gapi/controller/skillbox/cmarket/{list_sources,list_skills,refresh,install_skill}.a.go` | 4 端点 |
| `api-server/internal/gapi/controller/skillbox/cmarket/cmarket_test.go` | 路由注册校验 |
| `api-server/cmd/bootstrap/entities.go` | 注册新表到 AutoMigrate |
| `api-server/internal/gapi/router/routers_import.go` | 挂 cmarket |
| `frontend/src/api/skillbox/market.js` | API 封装 |
| `frontend/src/views/MarketView.vue` | 市场页(源切换/搜索/详情/装) |
| `frontend/src/App.vue` | 加 Market tab |

**接口**

| Method | Path | 行为 |
| --- | --- | --- |
| GET  | `/api/skillbox/market/sources` | 列已注册三方源(seed 默认 skillhub + skills.sh) |
| GET  | `/api/skillbox/market/skills` | 列 market_skills 缓存(支持 source_id / keyword 过滤 + 分页) |
| POST | `/api/skillbox/market/refresh` | 触发一个源刷新(同源并发被 ignore) |
| POST | `/api/skillbox/market/install` | 把三方 skill 装到本地 store(scope=global/project) |

**实现要点**

- `MarketAdapter` 接口(Discover / Detail / Download)+ 默认 registry + init() 自注册
- skillhub 适配器:GET `/api/v1/skills` + `/api/v1/skills/<id>/skill.md`;失败/非 JSON 走 knownFallback(内置 3 条样例),保证 UI 始终有内容
- skillssh 适配器:解析 catalog 页 "owner/repo@skill" 正则;Download 走 GitHub raw 4 个候选路径(main/master × skills/<name>/ 与 <name>/);fallback 用 4 条 known catalog
- 缓存层 `market_skills` 表:每行 = (source_id, remote_id) 唯一,RefreshFromSource 走 upsert
- orchestrator 防止"刷新风暴":同一 sourceID 短时间内并发触发走 ErrSourceBusy(409)
- orchestrator 把 base_url 解析交给 `ConfigJSON`(可被用户在 Settings 里覆盖)
- smarket.Install 走两步:orchestrator.DownloadFromSource 拿 canonical → 复用 sskill.Service.Create 写盘+DB;失败原子回滚
- 4 个 controller 全部走 `ginp.BindParamsHandler`(无 SSE,无手动 flush)
- 前端 MarketView:源 chip 切换 / keyword 搜索 / 刷新按钮 / 装按钮(scope 暂只放 global);refresh 完成后立刻 refetch
- 路由注册走 routers_import.go blank import(`_ "...cmarket"`)

**踩坑(详见 [开发报告.md §2.15~2.18](./开发报告.md))**

- §2.15:`go test` 沙盒里 `httptest.NewServer` 失败(端口绑定被禁)→ 改 `http.RoundTripper` mock,不监听端口
- §2.16:init() 在 smarket 之外的包调用 `skillmarket.Register` 不会跑(没有 import 关系)→ 在 smarket package 加 blank import 触发 init
- §2.17:orchestrator 创建时 `registry: &defaultRegistry` 把 `*Registry` 双重指针化 → 改成 `defaultRegistry`
- §2.18:skillhub entity 没有 `CreatedAt/UpdatedAt` 字段 → DefaultSources 不写这两个,直接走 GORM 自身

### 3.7 下一步入口(第 8 步)

- `internal/skillmarket/` 包:支持 skillhub.cn / skills.sh 两个三方源(读目录索引 / 拿单 skill 详情)
- 2 张新表(已有 `market_sources` 表;新增 `market_skills` 缓存表)
- 4 个端点:`GET /api/skillbox/market/sources` / `POST /api/skillbox/market/refresh` / `GET /api/skillbox/market/skills` / `POST /api/skillbox/market/install`
- 前端 MarketView:源切换 / 搜索 / 详情 / 一键安装到 store
- build + test + 提交

- `internal/skilltester/` 包:静态 lint(frontmatter 完整性 / 文件路径合法性)+ 脚本执行(沙箱跑 skill 自带 test.sh)+ AI 模拟走查(给 AIPanel 加一个 "跑一下试试" 入口)
- 3 个端点:`POST /api/skillbox/skills/test/run` 触发;`GET /api/skillbox/skills/test/:id` 看历史;`GET /api/skillbox/skills/test/:id/result` 拿结果
- 前端 SkillsView 加 "测试" 按钮 + 结果面板
- 跑通 build + test + 提交

### 3.8 第 8 步 — 应用与同步(Apply / Undo / 批量 / 更新检测)

提交 `c73aeff` Skill Box 应用与同步(Apply / Undo / 批量 / 更新检测)。

**新增 / 修改文件**:

| 路径                                                                                  | 类别                |
| ------------------------------------------------------------------------------------- | ------------------- |
| `api-server/internal/skillapp/{types,applier,batch,undo,updater,export_test}.go`     | 核心 Apply / Undo / Batch / Updater |
| `api-server/internal/skillapp/{applier_test,batch_test,undo_test,updater_test}.go`     | skillapp 核心测试(22 用例) |
| `api-server/internal/gapi/service/skillapp/sskillapp/skillapp.s.go`                   | 业务层:Apply / Batch / Undo / List / CheckUpdates |
| `api-server/internal/gapi/service/skillapp/sskillapp/{skillapp.s_test,export_test}.go` | sskillapp 业务测试(11 用例) |
| `api-server/internal/gapi/controller/skillbox/cskillapply/{apply_skill,apply_batch,undo_skill,list_applies,check_updates,cskillapply_test}.a.go` | 5 端点 + 路由测试(1 用例) |
| `api-server/internal/gapi/router/routers_import.go`                                  | 注册 cskillapply 包 |
| `api-server/internal/gapi/model/skillbox/mskillapply/skill_apply.f.go`                | 新增 FieldAppliedAt |
| `frontend/src/api/skillbox/skill_apply.js`                                           | 前端 apply / undo / list / updates HTTP 封装 |
| `frontend/src/views/SkillsView.vue`                                                   | SkillsView 加 apply bar + 历史面板 + update 徽章 |
| `docs/project/进度.md` / `docs/project/开发报告.md`                                  | 同步本文档 + 测试报告 |

**端点列表(5 个)**

| Method | Path                                      | 说明                                |
| ------ | ----------------------------------------- | ----------------------------------- |
| POST   | `/api/skillbox/skills/apply`              | 单 skill 落 1~N 工具,失败不互锁    |
| POST   | `/api/skillbox/skills/apply/batch`        | 批量 (skill × tool 笛卡尔积),atomic 模式失败整体回滚 |
| POST   | `/api/skillbox/skills/apply/undo`         | 撤销一条 apply(按 apply_id)         |
| GET    | `/api/skillbox/skills/apply/list`         | 列 apply 历史,按 applied_at desc    |
| GET    | `/api/skillbox/skills/updates`            | 本地 vs 三方市场缓存版本对比        |

**关键设计点**

- **Pre-snapshot JSON**:每次 apply 把目标目录(已存在 / 文件列表 / post_files)记到 `entity.SkillApply.PreSnapshot`,Undo 时按这份 JSON 严格还原(写回 pre 里的文件 / 删除 post 里的文件)
- **原子回滚**:BatchApplier 用 goroutine 并行回滚,任一失败不阻塞其他;`atomic=true` 模式时第 N 步失败 → 整体回滚已成功的(Step 7 安装项目级 skill 时配合使用)
- **scope 路径解析**:global scope 走 adapter 的第一个 DiscoverPaths;project scope 把相对路径拼到 `~/.skillbox/projects/<id>/`(Step 9 配合 sproject 换成真项目根)
- **更新检测匹配规则**:优先 `source=market + source_ref="<source>:<remote>"` 匹配;fallback 到同名匹配 + 取最大版本;semverCmp 走 x.y.z 简化解析(pre-release / build 简化掉)
- **Status 状态机**:`applied` (落地) / `rolled_back` (已撤销) / `failed` (失败) — Undo 只接受 `applied` 状态

**测试用例(34 用例,全过)**

- skillapp 核心 22:`ApplyOne` 6 + `BatchApplier` 4 + `Undo` 5 + `Updater` 5 + `SemverCmp` 1 + 隐藏 trace 1
- sskillapp 业务 11:Apply / Batch / Undo / List / CheckUpdates 链路 + sentinel 错误
- cskillapply 路由 1:5 端点都在 `ginp.GetAllRouter()` 表里
- 详细测试报告 → [开发报告.md §3.8](./开发报告.md#38-step-8-测试报告2026-06-22)

**前端 SkillsView 新增 UI**

- 顶部 "Apply 目标工具" 下拉(默认 codex)+ "检测更新" 按钮 + update 徽章
- 表格加 "更新" 列(显示 source 类型)+ "应用" 按钮(单 skill 落到所选工具)
- "最近 Apply 历史" 面板(取该 skill 最近 5 条 apply,可单条撤销)

### 3.9 第 9 步 — 快照与回滚(tag / diff / rollback)

提交 `e876318` Skill Box 快照与回滚(tag / diff / rollback)。

**新增 / 修改文件**:

| 路径                                                                                  | 类别                |
| ------------------------------------------------------------------------------------- | ------------------- |
| `api-server/internal/skillaudit/{types,diff,tag}.go`                                 | 核心:tag 构造 / diff(LCS) / 验证 |
| `api-server/internal/skillaudit/{types_test,diff_test,tag_test}.go`                  | skillaudit 核心测试(19 用例) |
| `api-server/internal/gapi/service/skillaudit/sskillaudit/skillaudit.s.go`             | 业务层:CreateTag / List / Delete / Diff / Rollback |
| `api-server/internal/gapi/service/skillaudit/sskillaudit/skillaudit.s_test.go`        | sskillaudit 业务测试(11 用例) |
| `api-server/internal/gapi/service/skill/sskill/export_test.go`                       | 给 sskill 暴露 GetDBForTest(白盒 export) |
| `api-server/internal/gapi/controller/skillbox/ctag/{create_tag,list_tags,delete_tag,diff_tag,rollback_tag,ctag_test}.a.go` | 5 端点 + 路由测试(1 用例) |
| `api-server/internal/gapi/router/routers_import.go`                                  | 注册 ctag 包 |
| `frontend/src/api/skillbox/tags.js`                                                  | 前端 tag / diff / rollback HTTP 封装 |
| `frontend/src/views/SkillsView.vue`                                                  | SkillsView 加 Tag 面板 + Diff 视图 |
| `docs/project/进度.md` / `docs/project/开发报告.md`                                   | 同步本文档 + 测试报告 |

**端点列表(5 个)**

| Method | Path                                          | 说明                                  |
| ------ | --------------------------------------------- | ------------------------------------- |
| POST   | `/api/skillbox/skills/tags/create`            | 打 tag:从 skillstore 读当前文件 → 固化为 skill_file_snapshots |
| GET    | `/api/skillbox/skills/tags/list`              | 列 skill 的所有 tag(按 created_at desc) |
| POST   | `/api/skillbox/skills/tags/delete`            | 删 tag(包括它的 file_snapshots)        |
| GET    | `/api/skillbox/skills/tags/diff`              | 两视图 diff(0 = current,>0 = tag) |
| POST   | `/api/skillbox/skills/tags/rollback`          | 回滚到 tag(自动打 _pre_rollback_<ts> 隐式 tag) |

**关键设计点**

- **Tag 内容**:从 skillstore.Load 读出当前所有文件 → 走 `skillaudit.BuildTag` 算 SHA-256 → 写 `entity.SkillTag` + `entity.SkillFileSnapshot`(每文件一行)
- **Diff 算法**:文件级按 path 对齐 + 行级 LCS;context 折叠为"紧邻 changed 行的 1 行",中间用 `...` 占位
- **Diff 排序**:added → removed → modified → unchanged(每类内 path 字典序)
- **回滚原子性**:
  1. 先打一个 `_pre_rollback_<RFC3339-紧凑版>` 隐式 tag(覆盖当前状态,失败可回退)
  2. 走 `sskill.Service.Update` 覆盖写 skillstore(把目标 tag 的 files 写回)
- **is_implicit 字段**:隐式 tag 可手动删除(`is_implicit` 只是标记,不影响删除权限)
- **uniqueIndex**: `(skill_id, tag)` 唯一约束 — 同 skill 下 tag 名不能重复(用 `idx_skill_tag_unique` 兜底)

**测试用例(31 用例,全过)**

- skillaudit 核心 19:ValidateTag 合法/非法(空格/超长/路径遍历字符) / HashContent 稳定 / FileMap / SnapFileMaps union / Diff added+removed / Diff modified / Diff unchanged / Diff 排序 / LinesDiff context 折叠 / LinesDiff added+removed / LinesDiff empty / BuildTag 5 case / ImplicitPreRollbackTag 格式 / FilesFromTagged 往返
- sskillaudit 业务 11:CreateTag OK / 错 skill / 错 tag / List 空 / List 多 / Delete OK / Delete 0/999(未找到) / Diff current vs tag / Rollback OK(自动 pre-rollback + 恢复内容) / Rollback 0
- ctag 路由 1:5 端点都在 `ginp.GetAllRouter()` 表里,method 对得上

**前端 SkillsView 新增 UI**

- 表格行加 "Tag" 按钮 → 打开"Tag 管理"面板(头部显示 skill name@version + tag 数 + 关闭)
- 面板内:打 tag 表单(2 input + 按钮)+ "Diff" 下拉(左/右各一个,选项 0=current + 所有 tag) + 列表(每行:id / name / message / time / vs current / 回滚到此 / 删)
- 选中 tag 列表项 → "vs current" 立即加载 Diff 结果
- Diff 视图:文件卡片(按 kind 上色)+ 行级显示(left_no|right_no + text,加色背景)
- 隐式 tag 行底色变浅黄,带 "[implicit]" 后缀

**端到端流(手动,记录在 §3.9)**

- 编辑一个 skill → 点 "Tag" → 输入 `v1` + 描述 → 打 tag → 列表多一条
- 改 skill 内容 → 点 "v1" 行的 "vs current" → Diff 面板展示 modified 文件 + 行级 added/removed
- 点 "回滚到此" → 二次确认 → 自动打 `_pre_rollback_<ts>` 隐式 tag + 把 v1 的内容写回 store → 编辑器看到内容已恢复
- 列表多一条 implicit tag(浅黄底)→ 可以点 "删" 删掉


### 3.10 第 10 步 — 导出/导入包 + 审计日志(2026-06-22)

**新增 / 修改文件**

| 路径                                                                                  | 类别                |
| ------------------------------------------------------------------------------------- | ------------------- |
| `api-server/internal/skillpkg/{types,exporter,importer}.go`                            | 核心:zip 编/解码 + 选条目装入 |
| `api-server/internal/skillpkg/{exporter_test,importer_test}.go`                        | skillpkg 核心测试(12 用例) |
| `api-server/internal/gapi/service/skillpkg/sskillpkg/skillpkg.s.go`                    | 业务层:BuildExport / ParseManifest / Import |
| `api-server/internal/gapi/service/audit/saudit/audit.s.go` + `audit.s_test.go`          | 审计 service + 1 占位单测 |
| `api-server/internal/gapi/controller/skillbox/cskillpkg/{export,import,preview}_package.a.go` | 3 端点(export 直接 octet-stream / import 接 octet-stream / preview) |
| `api-server/internal/gapi/controller/skillbox/cskillpkg/cskillpkg_test.go`            | 路由测试(1 用例) |
| `api-server/internal/gapi/controller/skillbox/caudit/{list_audit_logs,get_audit_stats}.a.go` | 2 端点 |
| `api-server/internal/gapi/controller/skillbox/caudit/caudit_test.go`                  | 路由测试(1 用例) |
| `api-server/internal/gapi/router/routers_import.go`                                    | 注册 cskillpkg / caudit |
| `frontend/src/api/skillbox/{onboarding,audit}.js`                                       | 新 API 封装 |
| `frontend/src/views/{OnboardingView,AuditView}.vue`                                    | 新视图(向导弹 3 步 / 概览 + 列表) |
| `frontend/src/App.vue`                                                                 | 重写:侧栏 + 顶栏 + 5 个 nav + 统计徽章 + 全局样式 token |
| `frontend/src/views/{Projects,Skills,Market}View.vue`                                 | 适配新全局样式,加 loading / 空态 / 卡片化 |
| `docs/project/{进度,开发报告}.md`                                                       | 同步本文档 + 测试报告 |

**端点列表(5 个)**

| Method | Path                                          | 说明                                  |
| ------ | --------------------------------------------- | ------------------------------------- |
| POST   | `/api/skillbox/pkg/export`                    | 选 skill 列表 → .skillbox zip 字节流(下载) |
| POST   | `/api/skillbox/pkg/preview`                   | 接 .skillbox zip,只解析 manifest 返回索引(导入前预览) |
| POST   | `/api/skillbox/pkg/import`                    | 接 .skillbox zip + query(target_scope / project_id / skills=),装入 store |
| GET    | `/api/skillbox/audit/logs`                    | 列表 + 过滤(actor / action / target_type) + 分页 |
| GET    | `/api/skillbox/audit/stats`                   | 总数 + 按 action / actor 分布 |

**关键设计点**

- **.skillbox 容器**:就是一个 zip,根 `manifest.json`(pkg_format=skillbox.v1 + skill 索引)+ `skills/<key>/` 子目录(子目录里 `skill.yaml` + 全文件);不引第三方 zip 库,用 `archive/zip` + `gopkg.in/yaml.v3`
- **CanonicalProvider 抽象**:exporter 需要"按 (scope, project_id, name, version) 取 canonical",但不想直接 import `sskill.Service` 防循环,所以定义 `skillpkg.CanonicalProvider` 接口,业务层 `sskillpkg` 写 adapter 桥过去(同款 §2.16 init 模式防坑)
- **SkillInstaller 抽象**:importer 反向同理;`sskill.Service.Create` 已经有 store.Save 失败回滚物理文件的能力(见 Step 4 报告),直接复用
- **Raw body 协议**:export/import/preview 三个端点都不走 `BindParamsHandler`,签名是 `func(*gin.Context)`,自己 `ShouldBindJSON` / `io.ReadAll`,跟 §2.11 chat_stream 同款(避免自动 JSON 包装或自动 200)
- **审计写入端点不在路由层**:saudit.Write 只暴露给同进程 service(后续 apply / undo / import / rollback 各自在成功路径加 1 行 `saudit.Write` 即可,目前尚未串联,留 Step 12 收尾)
- **前端 Onboarding 向导**:3 阶段(状态 / 扫描+勾选 / 完成),默认全选、空 items=全部;走 `?target_scope=global` 直装
- **前端 Audit 概览**:3 张统计卡片 + 过滤栏 + 列表;后端未就绪时(401/404)显示"待就绪"占位

**测试用例(14 用例,全过)**

- skillpkg 核心 12:`BuildBytes_Empty` / `_OneSkill_OK`(完整断言 zip 结构 + manifest + skill.yaml)/ `_PartialFailure` / `_AllFail`;`ParseManifest_Ok` / `_NotZip` / `_NoManifest`;`Install_AllOK` / `_Selective` / `_PartialFail` / `_UnknownKey` / `_BadScope` / `_ProjectRequiresID`
- saudit 1:Service 可构造 + model() 非 nil
- cskillpkg 路由 1:3 端点都在 `ginp.GetAllRouter()` 表里,method 对得上(POST × 3)
- caudit 路由 1:2 端点都在 `ginp.GetAllRouter()` 表里(GET × 2)
- 详细测试报告 → [开发报告.md §3.10](./开发报告.md#310-step-10-测试报告2026-06-22)

**前端 UI 全面升级(2026-06-22)**

- 整体从"单 tab 顶导"改为"应用风格"侧栏 + 顶栏:左侧 240px 深色侧栏(brand + 5 个 nav + 后端健康点),右侧浅色主区(顶栏含面包屑 + 3 个统计徽章)
- 5 个 nav:Skills / Projects / Market / Onboarding / Audit
- 全局样式 token(`--primary` / `--success` / `--danger` / `--warning` / `--bg` / `--text` 等)抽到 App.vue
- 通用元素:input / select / textarea / button(包含 `.primary` / `.danger` / `.link` / `.ghost` / `.sm` 变体)/ card / empty-state / spinner
- SkillsView / ProjectsView / MarketView 全部卡片化,空态加图标 + 引导文案
- 新增 OnboardingView:3 步 wizard(状态/扫描+勾选/完成)
- 新增 AuditView:3 张统计卡 + 过滤栏 + 列表(后端就绪后自动启用)
- 顶栏"刷新"按钮可手动重拉统计

**端到端流(手动,记录在 §3.10)**

- 启动 web 二进制,浏览器开 `/`:左侧深色栏,5 个 nav,右上角 3 个统计徽章(Skills / Projects / Tools)
- 切到 Onboarding:看到 5 个 adapter 状态卡 → 点"开始扫描" → 扫描结果按 tool 分组,默认全选 → 选 1 个 → 点"导入到 store" → 跳到"完成"面板看到 OK/Failed 统计
- 切到 Audit:目前 0 条记录(尚未串联 saudit.Write),显示空态;切到 Skills 跑一次 apply/undo/tag 等操作后,Step 12 收尾时接入 saudit.Write,这里会自动出现日志
- 切到 Skills:卡片化布局,空态显示"该 scope 下还没有 skill,点"+ 新建 Skill"开始,或去 Onboarding 从已装工具导入"
- 浏览器窗口缩窄(测试响应式):侧栏固定 240px,主区 overflow:auto;切到 1024px 以下时 grid 仍能横向滚动,不会破版

**已知限制(留 P1 / Step 12)**

- .skillbox 二进制文件:v1 只支持文本文件(zip 里 content = `[]byte` 全读),二进制走 base64 + Encoding 字段 P1 再加
- .skillbox 签名/加密:目前包是明文 zip,可被任意第三方 skillbox 实例读取;签名 + 可选加密 P1(用 0.1.0 公开的 secret 字符串 + AES-GCM)
- preview 不显示文件列表(只显示 manifest 索引),前端要"看包内文件树"需多一个端点;Step 12 评估
- AI 测试结果暂未自动写 audit;Step 12 与 test_run 端点一起接入

### 3.11 Step 11 收尾 — audit 写入串联(2026-06-22)

详细报告见 [开发报告.md §3.11](./开发报告.md#311-step-11-收尾--audit-写入串联2026-06-22)。摘要:

- **目标**:把 §3.10 P1 列表里 "audit 写入未串联" 抹掉;在 sskillapp / sskillaudit / sskillpkg 三个 service 的成功 + 失败路径各埋 1 行 `saudit.Write`
- **13 种 audit 事件**:`apply` / `apply_failed` / `undo` / `undo_failed` / `tag_create` / `tag_create_failed` / `tag_delete` / `rollback` / `rollback_failed` / `export` / `export_failed` / `import` / `import_failed`
- **关键改动**:`sskillapp.Service` / `sskillaudit.Service` / `sskillpkg.Service` 构造函数加 `dbWrite / dbRead`;`skillapp.ApplyResult` 加 `ApplyID uint` 字段(供 audit 拿 apply_id);3 个 cskillpkg controller 同步加 db 参数
- **actor 暂填 `"system"`**(P1 接入登录后替换为当前用户名);payload 走 JSON 字符串
- **新增 5 个端到端测试**:`sskillapp.TestApply_WritesAuditLog` / `TestUndo_WritesAuditLog` / `sskillaudit.TestCreateTag_WritesAuditLog` / `TestRollback_WritesAuditLog` / `sskillpkg.TestExportImport_WritesAuditLog`
- **回归**:全量 23 个包测试全过,build 三件套干净通过
- **手动端到端**:Step 12 收尾时跑一次"创建 skill → apply → undo → 打 tag → rollback → export → import"完整链路,确认 Audit 页 by_action 出现预期事件分布

### 3.12 Step 11 收尾 — Onboarding 跳 Skills + export/import 端到端(2026-06-22)

详细报告见 [开发报告.md §3.12](./开发报告.md#312-step-11-收尾--onboarding-跳-skills--exportimport-端到端2026-06-22)。摘要:

- **目标**:把 §3.10 P1 列表里剩余 2 条("Onboarding 完成页"去 Skills 页查看"按钮未实现 jump event 路由切换" + "端到端 export → import 链路未跑")一起抹掉
- **前端 — 事件总线**:`App.vue` 新增轻量 `eventBus` IIFE(`on / off / emit` 形似 mitt),`provide('appBus', bus)`;`OnboardingView` `inject` 后 `appBus.emit('switch-tab', 'skills')` 走跨组件跳转。兜底用 `window.addEventListener('skillbox:switch-tab', ...)` + `CustomEvent` 防止 inject 失败时按钮失效
- **前端 — Onboarding 按钮**:从占位 `$emit('jump', 'skills')` 改为 `goSkills()` 方法,统一走 bus
- **端到端**:`scripts/e2e_pkg_roundtrip.sh`(11 步:create → list → export → delete → list → preview → import → list → get detail → audit stats → audit logs)真跑过 web + nc 全链路,PASSED。`export` 产物 976 字节 zip,`import` 后 audit_log 出现 export=1 + import=1
- **新增文件**:`scripts/e2e_pkg_roundtrip.sh` + `configs.e2e.yaml`(后者不入仓,加 .gitignore)
- **回归**:前端 `npm run build` 干净通过(58 modules,152.14 kB);e2e 跑的就是真 web 链路,等价后端回归
- **踩坑**:`configs.ServerConfig` 实际只有 `Port` 一个字段,我猜的 `api_host` / `api_port` 等不存在,改成 `server.port: "8084"` 即可;`installed_ids=[2]` 不是 `[1]`,SQLite `AutoIncrement` 在 delete 后不复用 ID,正常行为

### 3.13 Step 11 — 预置 skill 库(2026-06-22)

- **目标**:首次安装内置 6 个高质量 skill(code-review / commit-msg / debug-helper / doc-generator / unit-test-gen / perf-opt),让新装用户开箱即用
- **实现路径**:
  - `internal/skillbundle/presets/<key>/SKILL.md` + `examples/*.sh`(6 套,共 13 个文件):每个 SKILL.md 都有合法 frontmatter(`name` / `version: 0.1.0` / `description` 长度 [10,500] / `triggers` 1-10 个 / `author: Skill Box` / `license: MIT` / `target_tools` 覆盖全部 5 个工具)
  - `internal/skillbundle/bundle.go`:`go:embed presets` 把 6 个目录整体打入二进制;`LoadAll()` / `LoadOne(subpath)` 走 `skilladapter.ParseSkillMD` 把 SKILL.md 转成 `Canonical`,然后扫 `examples/*.sh` 按 Path 注入 Files
  - `internal/skillbundle/bundle_test.go`:7 个白盒测试(load 全过 / 顺序稳定 / manifest 合法 / 名字正则 / 有 SKILL.md / 有 examples/.sh / 不存在路径报错)
  - `cmd/bootstrap/bundle_seed.go`:`SeedBundledSkills()` 在 `StartDB` 之后调;走 `sskill.Create` 同一条路径(落盘 + 落库),`source="bundle"` / `source_ref="skillbox/builtin"` 标识来源;幂等(同 (scope, name, version) 跳过)
  - `cmd/bootstrap/bootstrap.go`:`Boot()` 在 `StartDB` 后调 `SeedBundledSkills()`,在 `StartTask` 之前 — seed 是数据初始化不是定时任务
- **回归**:7 个 bundle 测试全过;build 干净;手动 e2e 跑 2 次启动:第 1 次 `inserted=6 skipped=0 failed=0`,第 2 次 `inserted=0 skipped=6 failed=0`,DB 6 条不变
- **前端可见**:`/api/skillbox/skills?scope=global` 直接列出 6 条 `source=bundle` 的 skill;卡片详情展示完整 manifest_json
- **P1 入口**:Settings → "内置 skill" 开关可关掉 seed(适合只用第三方 skill 的高级用户);重新打开后下次启动恢复


