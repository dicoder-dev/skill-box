# 首页打 tag 部分隐藏,点击版本号弹标签弹窗

**日期:** 2026-06-25
**状态:** 进行中

## 1. 需求

首页右侧详情区"标签"section 不再显示具体 chip 列表,改为点击版本号(`<code class="detail-version">@x.y.z</code>`)弹出标签管理弹窗。

具体改动:
- 删掉 `frontend/src/views/SkillsView.vue` 模板里的"标签列表"section(原本展示 `currentTagList` chip 列表 + "新建"按钮)
- `currentTags` / `currentTagList` 这些 state 仍保留(详情 chip 显示原本用的是这些数据),但目前详情区没有"标签 chip"展示,所以保留仅供弹窗使用
- 顶栏的"打标签"图标按钮(`mdi:tag-outline`)已存在,继续保留作为备选入口
- `detail-version` 元素加上 `@click="openTagDialog"`,并加 cursor: pointer + hover 反馈(下划线或变色),让用户知道可点

## 2. 任务列表

- [x] 删掉标签 chip 列表 section(原 1201-1220 行)
- [x] detail-version 加点击事件 + 视觉反馈
- [x] 自测(前端 build)
- [ ] 提交 + 推送

## 3. 执行进度

- 18:07 阅读 SkillsView.vue 理解现状
- 18:07 准备开 task + 改代码
- 18:09 改完 + 前端 build 通过(880ms)

## 4. 问题与方案

无

## 5. 需求回流

无

## 6. 测试报告

**自测时间:** 2026-06-25 18:09
**自测人:** AI(本轮 Claude)
**自测范围:** SkillsView.vue 模板 + 样式

### 6.1 自动化测试
- 前端 `npm run build` 结果: ✅ 通过(880ms)
- 前端 `npm run lint`: 未跑(项目无 lint 配置)

### 6.2 手工 / 接口验证(代码层确认)
- [x] 详情区不再展示标签 chip 列表 → `grep detail-section` 确认"标签列表"section 已删
- [x] 版本号点击触发 openTagDialog → 模板上加了 `@click="openTagDialog"` + `@keyup.enter`
- [x] 版本号视觉反馈(可点) → 新增 `:hover` 背景变色 + `:focus-visible` 蓝色 outline
- [x] 顶栏 tag-outline 按钮仍保留作为备选入口 → 未删

### 6.3 边界 / 异常
- [x] current 为 null 时 detail-version 不渲染(v-if="!current" 包裹整个详情)
- [x] openTagDialog 内部有 `if (!current.value) return` 防御

### 6.4 自测结论
- 总体: ✅ 通过
- 遗留问题: 无

## 7. 总结

- 删了详情区"标签列表"section,不再展示 chip 列表
- 版本号 `@x.y.z` 现在是可点的(hover 变色 + 焦点 outline),点击 / 回车都弹标签管理弹窗
- 顶栏的 tag-outline 按钮保留作为备选入口
- openTagDialog 仍依赖 current,空态不渲染可点版本号

## 8. 改动的文件

### 8.2 修改
- `frontend/src/views/SkillsView.vue` — 删标签列表 section;detail-version 加点击 / 回车事件 + 视觉可点反馈

## 9. 对话轮次

### 9.1 对话轮次 (18:07)

> 用户原话:首页打 tag 部分不用显示,点击版本号弹出标签弹窗即可

- **本轮做了:** 删 `SkillsView.vue` 模板里"标签列表"section;`detail-version` 加 `@click="openTagDialog"` + `@keyup.enter` + role/tabindex,样式新增 hover/focus-visible 反馈;前端 build 通过。
- **本轮决定:** 顶栏 tag-outline 图标按钮保留作为备选入口(没删)。
- **本轮待办:** 提交 + 推送。
- **状态更新:** 任务列表 → 提交推送待办。