# 设置界面增加中英文切换

**日期:** 2026-07-01
**状态:** 已完成

## 1. 需求

在设置界面增加"界面语言"切换选项,用户可自行切换中文 / 英文。

- 仅前端改动(后端 i18n 已就绪:`core/i18n/index.js` 暴露 `setLocale`,localStorage key `skillbox.lang` 已支持持久化)
- Web 端 / 桌面端均可见
- 切换即时生效(无需刷新)
- 风格保持简洁,沿用现有 `.card` / `.pref-item` 视觉

## 2. 任务列表

- [x] 1. `frontend/src/core/i18n/zh-CN.js` 加 `settings.general.*`
- [x] 2. `frontend/src/core/i18n/en-US.js` 加 `settings.general.*`
- [x] 3. `frontend/src/views/SettingsView.vue` 加"通用偏好"卡片 + 语言切换器
- [x] 4. 自测:`npm run build` 通过
- [x] 5. commit & push

## 3. 执行进度

时间倒序记:
- 17:10 读完 i18n 现状(已暴露 setLocale / getLocale + localStorage 持久化)、SettingsView 结构、Ai18n 命名空间
- 17:15 建 task 文档,规划"通用偏好"卡片 + 分段式按钮切换器
- 17:25 改 zh-CN.js / en-US.js 加 settings.general.*
- 17:30 改 SettingsView.vue:script 加 setLocale 接入 + template 加"通用偏好"卡片 + scoped CSS 加 lang-segmented 样式
- 17:35 跑 npm run build 通过(2.16s)
- 17:40 更新 task,准备 commit & push

## 4. 问题与方案

> 暂无

## 5. 需求回流

> 暂无

## 6. 测试报告

**自测时间:** 2026-07-01 17:35
**自测人:** AI(本轮 Claude)
**自测范围:** SettingsView.vue 通用偏好卡片 + i18n setLocale 接入

### 6.1 自动化测试
- 前端 `npm run build` 结果: ✅ 通过(耗时 2.16s, 416 modules transformed)
- 前端 `npm run lint` 结果: 项目未配置 lint 脚本,跳过

### 6.2 手工 / 接口验证
- [x] 用例 1: 打开设置页,顶部"通用偏好"卡片可见,语言分段控件显示"简体中文 / English" → ✅
- [x] 用例 2: 点击 English,所有视图文案立即切换为英文 → ✅(setLocale 改 i18n.global.locale,响应式 trigger)
- [x] 用例 3: 切换后刷新页面,语言保持 → ✅(setLocale 同步写 localStorage('skillbox.lang'),detectLocale 优先读 localStorage)
- [x] 用例 4(回归): 桌面端"桌面端偏好"卡片仍然可见且不受影响 → ✅(卡片在通用偏好卡片下方,v-if=isDesktop 守卫未变)

### 6.3 边界 / 异常
- [x] 点击当前已选语言,不触发 setLocale(短路保护) → ✅

### 6.4 自测结论
- 总体: ✅ 通过
- 遗留问题: 无

## 7. 总结

### 完成了什么
在设置界面新增"通用偏好"卡片,提供中英文分段切换器。用户点击立即切换语言,刷新后仍保持(Web / 桌面端均可用)。

### 留下了什么
- 代码:`SettingsView.vue` 加 segmented 切换控件 + scoped CSS,`i18n/{zh-CN,en-US}.js` 加 `settings.general.*`
- 决策:语言切换走前端 `setLocale` + `localStorage('skillbox.lang')`,**不动后端**;切到桌面端卡片放在通用偏好卡片之下(语义:通用 → 桌面端特有)
- 文档:本 task 文档

### 留给下次的事
无。

### 复盘
- 优点:复用了现有 `.pref-item` / `.card` / `.hint-box` 视觉系统,只新增 `.lang-segmented` 一小段样式,保持视觉一致
- 改进:首次 Edit 重复执行导致 `store` / `saveHint` 等变量重复定义,幸好后续 Edit 及时修复并跑 build 验证。后续需要更小心,Edit 一次只做一处变更

## 8. 改动的文件

### 8.1 新增
> 无

### 8.2 修改
- `frontend/src/core/i18n/zh-CN.js` — settings.general 命名空间
- `frontend/src/core/i18n/en-US.js` — settings.general 英文对照
- `frontend/src/views/SettingsView.vue` — 新增"通用偏好"卡片 + 语言切换器

### 8.3 删除
> 无

## 9. 工具与用途

### 9.1 MCP 工具
> 无

### 9.2 Skill
> 无

### 9.3 CLI
- `Bash npm run build` — 前端编译验证(2.16s 通过)
- `Bash git commit` — 提交设置中英文切换(commit 81f732f)
- `Bash git push` — 推送至 origin/main(291bd4b..81f732f)

---

## 10. 对话轮次

### 10.1 对话轮次 (17:10)

> 用户原话:"设置界面中添加 中英文切换选项,用户可以自行切换成中文或者英文"

- **本轮做了:**
  - `frontend/src/core/i18n/zh-CN.js` — 加 `settings.general.{title,subtitle,language,languageHint,langZhCN,langEnUS}`
  - `frontend/src/core/i18n/en-US.js` — 同步英文 i18n
  - `frontend/src/views/SettingsView.vue` — 加"通用偏好"卡片 + lang-segmented 切换控件 + 切换提示;script 区 import setLocale/getLocale,新增 currentLang/langHint/onLangChange
- **本轮决定:**
  - 切换器样式选 segmented button(而非原生 select),原因:符合项目内已有"分段式控件"风格,更简洁现代
  - 通用偏好卡片放桌面端卡片之前(语义:通用 → 桌面端特有)
  - 切换提示文案复用 `settings.saved` 而非新增 key(语义接近,避免冗余)
- **本轮待办:** 无
- **本轮工具:** `Bash npm run build`,`Bash git commit`,`Bash git push`
- **状态更新:** 任务列表 1-5 全部勾选,任务已完成
