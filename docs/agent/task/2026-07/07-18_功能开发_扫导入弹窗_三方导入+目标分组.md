# 首页"扫导入"Tab 改造:从三方导入 + 目标分组

**日期:** 2026-07-18
**状态:** 进行中

## 1. 需求

用户要优化首页"导入技能"弹窗(`OnboardingImportDialog`),具体:

1. **新增「从三方导入」Tab**,放在首位,只放一个 URL 输入框,后端统一调度(skillhub.cn / skills.sh / GitHub)。
2. **「扫描工具」的名称更改为「工具」**,改为「从工具导入」(保持 tab 文字简洁)。
3. **从三方导入**功能:用户直接粘详情页 URL,系统自动识别市场并导入。
4. **后端新增统一调度管理接口**(决策:不新开 endpoint,扩展现有 `install-from-input` 加 `group_path`)。
5. **市场来源列举与引导**:把 Skill Hub / skill.sh / GitHub 三个市场源都列出来,带跳转官网,加操作提示。
6. **所有 Tab 共享「目标分组」选择器**(弹窗顶部 tab 上方一行),默认根分组。
   - 本次只让 market tab 实际生效,其他 3 tab 走默认分组(UI 上选择器对所有 tab 可见)。

## 2. 任务列表

- [x] 调研项目架构与市场模块
- [x] 调研 SkillsView / OnboardingImportDialog / 现有 import 面板
- [x] 调研后端 InstallFromInput / 派生逻辑
- [x] 写最终 plan(用户已确认 3 个关键决策)
- [x] 后端:扩展 `InstallFromInputInput.GroupPath` + 派生逻辑分支
- [x] 后端:扩展 `RequestInstallFromInput.GroupPath` + Swagger 注释
- [x] 前端:新增 `MarketImportPanel.vue`(sources 列表 + URL 输入 + conflict 弹窗)
- [x] 前端:新增 `GroupPathSelect.vue`(目标分组下拉)
- [x] 前端:改 `OnboardingImportDialog.vue`(4 tab 顺序、provide targetGroupPath)
- [x] 前端:改 i18n(zh-CN + en-US)
- [x] 前端:改 `market.js` 注释
- [x] 自测:后端 go test + 前端 npm run build
- [x] git commit × 2(后端 + 前端)
- [ ] git push(网络 SSL_ERROR_SYSCALL 持续失败,留给下次)

## 3. 执行进度

- 04:49 后端 commit 1:install_from_input.go + install_from_input.a.go,加 group_path 入参
- 04:49 后端 go test:GroupPath / DeriveGroupPath / SplitOwner / PullV2_GroupPath 全部通过(7 case,耗时 0.14s)
- 04:49 git push 失败:LibreSSL SSL_ERROR_SYSCALL in connection to github.com:443(网络问题,稍后重试)
- 04:50 前端 commit 1:MarketImportPanel.vue + GroupPathSelect.vue + OnboardingImportDialog.vue 改 + i18n × 2 + market.js 注释
- 04:51 前端 npm run build:✅ 通过(13.82s)
- 04:51 后端 go build ./...:✅ 通过(无输出,0 错误)
- 04:51 git push 又失败,持续网络问题

## 4. 问题与方案

- 问题 1:`TestResolveInstallInput_GitHubTreeURL` 测试预存在失败
  - 现象:5 个 subtest 全部 FAIL,RemoteID 期望 "anthropics/skills@pdf" 实际 "anthropics/skills@skills/pdf"
  - 定位:`git stash` 在 main 分支上单独跑同一组测试,结果同样失败。**预存在的测试 bug**,2026-07-10 改 `sanitizeSlug` 允许 `/` 后,旧测试期望没更新
  - 方案:不修复(不在本次任务范围),验证了"我的改动不引入新失败"
- 问题 2:git push 持续 `LibreSSL SSL_ERROR_SYSCALL`
  - 现象:两次重试都失败,GitHub 443 连接被 reset
  - 方案:留给用户下次手动重试 `git push`(网络问题,等几分钟或换网络)

## 5. 需求回流

(暂无)

## 6. 测试报告

**自测时间:** 2026-07-18 04:49-04:52
**自测人:** AI(本轮 Claude)
**自测范围:** 后端 smarket service + 前端 OnboardingImportDialog / MarketImportPanel / GroupPathSelect + i18n

### 6.1 自动化测试
- `go test ./internal/gapi/service/market/smarket/ -run "GroupPath|Normalize|DefaultGroupPath|DeriveGroupPath|SplitOwner"`:✅ 全部通过(7 case,0.14s)
- `go build ./...`:✅ 通过
- 前端 `npm run build`:✅ 通过(13.82s)
- 预存在失败 `TestResolveInstallInput_GitHubTreeURL`:与本次改动无关(main 分支 stash 后也失败)

### 6.2 手工 / 接口验证
- [x] 后端透传 `group_path` 编译过,Swagger 注释里说明 `group_path` 字段语义
- [x] 前端 build 通过,无 TypeScript / 模板错误

### 6.3 边界 / 异常
- [x] 后端 `GroupPath` 为空 → 走默认派生(`normalizeGroupPathForMarket(deriveGroupPath(...))`),与原行为一致
- [x] 后端 `GroupPath = "frontend/react"` → 走 `NormalizeGroupName` 二次规范化,Manifest.GroupPath 写规范化值,CreateGroup 建物理目录
- [x] 后端 `GroupPath = "../etc"` → 走 `NormalizeGroupName` 折叠,不会写出 store 根
- [x] 前端 GroupPathSelect 首次 mount 时 tree 为空 → onMounted 触发 tree.load(),失败时静默降级只显示「根分组」一项

### 6.4 自测结论
- 总体:✅ 通过(代码改动层面)
- 遗留问题:`git push` 持续网络问题,留给用户手动重试

## 7. 总结

### 7.1 完成了什么
- **后端**:`POST /api/skillbox/market/install-from-input` 扩展 `group_path` 入参,空时走默认派生,非空时走 `NormalizeGroupName` 二次规范化后落到指定分组目录。
- **前端**:OnboardingImportDialog 新增「从三方导入」tab(首位)、「扫描工具」改名「工具」,4 tab 共享顶部「目标分组」选择器。
- **新组件**:`MarketImportPanel.vue`(sources 列表 + URL 输入 + conflict 三选一 Modal)、`GroupPathSelect.vue`(递归枚举 group path,onMounted 懒加载 tree)。
- **i18n**:zh-CN + en-US 同步,新增 `onboarding.market.*` / `onboarding.targetGroup.*`,tab 顺序调整。
- 决策记录:用户选「扩展现有 install-from-input」而不是新开 unified-import,理由是减少接口蔓延;选「弹窗顶部 tab 上方一行」放分组选择器,4 tab 共享;选「内联复用 MarketView 模式」处理 conflict,不抽公共组件(改动量小)。

### 7.2 留下了什么
- 2 个 commit(后端 + 前端),本地 git 已有但 push 因网络问题失败
- 1 份 task 文档 + 2 份 i18n + 1 份前端 API 注释更新

### 7.3 留给下次的事
- `git push`(SSL_ERROR_SYSCALL 持续失败,等网络恢复重试)
- 修复预存在的 `TestResolveInstallInput_GitHubTreeURL` 失败(不在本次任务范围)
- 端到端冒烟:启动 wails3 dev,验证「从三方导入」tab 走通整链路(URL 输入 → 解析 → 下载 → 落指定分组 → toast → tree 刷新)

### 7.4 复盘
- **做得好**:从已有后端 InstallFromInput 入手扩展(避免新开 endpoint),前端 4 tab 共享 targetGroupPath 用 provide/inject 一行解,4 tab 顺序调整只在 OnboardingImportDialog 一处改。GroupPathSelect 用 useSkillTreeStore().tree 内联递归,不需要给 store 加新方法。
- **能改进**:MarketImportPanel 的 conflict Modal 应该用项目内 `<Modal>` 组件(目前是内联 div),但 `<Modal>` 是给全屏弹窗设计的,不太适合小型 confirm 弹窗,所以选了内联。如果后续其他面板也需要 conflict 弹窗,考虑抽公共组件。

## 8. 改动的文件

### 8.1 新增
- `frontend/src/components/MarketImportPanel.vue` — 三方市场导入面板(3 sources 卡片 + URL 输入 + conflict 三选一 Modal)
- `frontend/src/components/GroupPathSelect.vue` — 目标分组下拉选择器
- `docs/agent/task/2026-07/07-18_功能开发_扫导入弹窗_三方导入+目标分组.md` — 任务过程文件

### 8.2 修改
- `api-server/internal/gapi/service/market/smarket/install_from_input.go` — InstallFromInputInput 加 GroupPath,L212 派生逻辑分支
- `api-server/internal/gapi/controller/skillbox/cmarket/install_from_input.a.go` — RequestInstallFromInput 加 GroupPath + Swagger 注释
- `frontend/src/components/OnboardingImportDialog.vue` — tab 顺序、默认 tab、provide targetGroupPath、加 GroupPathSelect + MarketImportPanel
- `frontend/src/api/skillbox/market.js` — installFromInput 注释补 group_path
- `frontend/src/core/i18n/zh-CN.js` — tabs 重排 + 新增 onboarding.market.* / onboarding.targetGroup.*
- `frontend/src/core/i18n/en-US.js` — 同步

### 8.3 删除
(无)

## 9. 工具与用途

### 9.1 MCP 工具
- 暂无

### 9.2 Skill
- 暂无

### 9.3 CLI
- `Bash go test ./internal/gapi/service/market/smarket/ -run "GroupPath|..."` — 后端 GroupPath 相关单测(全部通过)
- `Bash go build ./...` — 后端全量编译(0 错误)
- `Bash npm run build` — 前端构建(13.82s 通过)
- `Bash git stash` — 验证预存在测试失败(确认与本次改动无关)
- `Bash git commit × 2` — 提交后端 + 前端改动
- `Bash git push × 2` — 推送(SSL_ERROR_SYSCALL 网络问题,失败)
