* 前端界面设计时，用户偏好简洁一点的界面，并且很反感AI感很强的界面 -20260623更新
* Claude Code 用户日常用的 skill 在 `~/.claude/skills/` 下(以 symlink 形式存在,目标在 `~/.agents/skills/` 等),不是 `~/.claude/plugins/marketplaces/`。adapter 必须同时扫这两个根,否则会漏掉用户的日常 skill。-20260623
* Anthropic 推行的 Agent Skills 开放标准,个人级路径是 `$HOME/.agents/skills/`,Claude / Codex / Trae 工具各自目录(`~/.claude/skills/`、`~/.codex/skills/`、`~/.trae/skills/`)通常以 symlink 形式指向 `~/.agents/skills/`。**adapter 写盘必须指向 `~/.agents/skills`**,不是工具特定目录,否则:(1) 工具实际不读取那个路径,apply 后技能不生效;(2) MkdirAll 会把 symlink 替换为实体目录,破坏用户原有目录布局。Cursor / OpenCode 例外,它们各自读取自己的 `~/.cursor/skills/` / `~/.config/opencode/skills/`。-20260629
* **项目级 scope 写盘路径每个工具不同,不能一刀切:** Claude → `<project>/.claude/skills/`(Claude Code 官方文档要求);Codex → `<project>/.agents/skills/`(Codex 项目级沿用 Agent Skills 标准);Trae → `<project>/.trae/skills/`(跟全局入口 `~/.trae/skills` 同名);Cursor → `<project>/.cursor/skills/`;OpenCode → `<project>/.opencode/skills/`。项目级没有 symlink 兜底,必须直接写工具自身目录,工具才能读到。新增工具 adapter 时务必先确认"项目级实际读哪个目录",不要假定跟全局入口一致。-20260629
* Claude / Codex adapter 不仅要扫 user 根,还要扫 system 根(plugins/marketplaces / .system / .curated),但 importer 必须用 category=user|system 区分。BaseAdapter.SystemPaths + IsSystemPath 是约定,新增工具时记得声明。-20260623
* 桌面端请求/响应日志在 `~/.skill-box/logs/2026-06/06-DD-request.txt`(每请求一段,含响应体)。bug 复现不出来时第一件事是翻这个文件,不要瞎猜。-20260623
* skillstore 是 `(StoreRoot, scope, name, version)` 唯一索引的物理存储,ListNames 只看 name 不看 version,前端做"已存在"判断用 name 即可。-20260623
* 跨 module 共享代码不能放在 `ginp-api/internal/`(被 Go internal 规则拒),要用 root module 的 `pkg/` 平面(如 `pkg/fsutil`)。-20260624
* Onboarding 入口在 phase2 直接展示扫描结果,跳过 phase1 status 步骤。状态信息改由 App.vue 顶栏 toolsReady/total 徽章提供,不要再回退到 phase1 表格。-20260624
* Onboarding 重复检测分两层:① 客户端 store 已存在(name 匹配,不分 version)→ 置灰 + "已存在"标签;② 跨工具同名互斥(同 name + version 不同 tool_id)→ 选一个后另一个自动取消。selectExclusiveByName 集中处理。-20260624
* 用户可能手动 cp skill 到 adapter 期望目录(不走 skillbox apply),scope-status 实时扫盘会命中,但 `skill_applies` 表里没记录。停用时必须走 `POST /api/skillbox/skills/apply/force-undo`(DB 有记录走标准 undo;无记录走 scope-status 删磁盘 + 插占位 rolled_back 行)。前端 `doUnapplyOne` 在 `listApplies` 返空时 fallback 调 `forceUndoApply`,不要直接 toast 失败。-20260625
* textarea 锁定行高要同时锁 `min-height` 和 `height`:单写 `height: 57px` 会被外层/全局 `min-height: 100px` 拉高(因为 min-height 优先级高于 height)。命名上用具体语义类(.desc-editor/.triggers-editor),在外层通用规则上用 `:not(.具体类)` 排除,不要图省事写 !important。-20260627
* Vue scoped CSS 编译时只给当前 SFC 模板里的元素加 `[data-v-xxx]`,**子组件根类透传上来的 DOM 不会被加 attribute**,所以全局/父级同名类规则能覆盖子组件 scoped 规则。排查 CSS 冲突不能只看 scoped 内部,也要看全局 style.css 和父级 .editor-field-full 等通用类。-20260627
* "导入型"流程优先走"系统文件夹选择对话框 + 服务端启发"两步:桌面端 wails v3 用 `app.Dialog.OpenFile().CanChooseDirectories(true).CanChooseFiles(false).PromptForSingleSelection()` 拿路径;服务端 `fsutil.InspectProject(path)` 启发 name(basename)/ alias(slugify)。前端在 platform 抽象里包成 `fs.pickFolder()` / `fs.inspectProject(path)`,Web 端降级到 501 + error 由调用方处理。-20260627
* `wails3 dev` **不会自动监听 Go 文件变更后重启**。配置里只有 `watched_extension: *.go` 用来识别 wails3 自己的 watched 路径,但没绑 rebuild 触发器。新加 controller / service / route 后必须手动 `pkill -f "wails3 dev"` + 重新跑 `wails3 dev -config ./build/config.yml -port 9245`,否则新路由会一直 404,排查时第一件事看 `~/.skill-box/logs/YYYY-MM/MM-DD-error_request.txt` 的 404。-20260627
* `wails3 dev` 模式下 Vite HMR 生效(前端源码改动能热替换),但 **Go 端走的是磁盘上 `bin/skill-box.dev.app/Contents/MacOS/skill-box` 这一个进程的代码段**,新路由必须在重启后才有;另外 `frontend/dist/`(根目录的)也不是 dev 用的 dev 用的源是 Vite 内存里 HMR 过的源码,**但 `api-server/cmd/web/frontend/dist/` 是 web 单进程部署用的**,改完前端记得 `npx vite build` 同步过去,否则 web 跑的是旧 dist。-20260627
* `ginp` 路由包装函数选择规则:`BindParamsHandler(handler, &Struct{})` 用来"handler 需要解析 body 参数"的场景,内部会构造 `[ctx, struct]...` 参数 reflect.Call(handler);**handler 不要 body 参数时(只有 `func(c *ContextPlus)`)必须用 `BindHandler(handler)`**,否则空 struct 占位会触发 `reflect: Call with too many input arguments` panic,被 RecoveryMiddleware 吞成 500。参考:`/api/desktop/clipboard/text`、`/api/desktop/open-external`、`/api/desktop/window/*` 全是 BindHandler 风格。-20260627
* 前端 entity 字段约定是**小写 JSON**(后端 entity tag 就是 `id` / `name` / `alias` / `root_path` 等小写蛇形),不要写 `p.ID` / `p.Name` / `p.Alias` 这种大驼峰——大小写错配会让 Vue 模板渲染为空(整行空白),排查时第一件事看 `~/.skill-box/logs/...-request.txt` 的"响应体"原始 JSON。SkillsView 等其它视图都用小写,新写视图前先 grep 看一眼现有约定。-20260627
* **配色禁用紫色作为主色**:紫色(violet / indigo / purple) AI 感很强,只能作为辅助配色(badge / stat)。主色 / CTA / 主题渐变主色 只能用 蓝 / 绿 / 橙 / 红 / 中性灰。新建 view、改主题前先看这条;已有的项目级紫色 badge (`--accent-violet`)、App.vue stat-badge-violet 不动,但 MarketView 等 view 的主题色若用紫色需要主动建议替换。-20260701* 工具自定义图标(2026-07-02):后端 e_tool 加 icon_file 字段(纯 basename,存 ~/.skill-box/tool-icons/<name>),不存绝对路径;seed 阶段 //go:embed builtin-icons/* 写到磁盘;前端 ToolIcon.vue 组件按"icon_file 优先 mdi_icon 兜底"渲染。后续若做新工具,seed 加 IconFile 字段 + 在 api-server/internal/toolseed/builtin-icons/ 放图即可。**已知问题**:web 模式下 SPA fallback 对 /api/files/tool-icons/*.png 的优先级有 bug,先用 `:filename` 段绑定规避;完整方案改 server.New 的 NoRoute 白名单(只对前端 SPA path fallback)。

* canonical 唯一来源字段约定(2026-07-03):skill 的"磁盘根真实路径"必须写到 `Canonical.SourceDir`(EvalSymlinks 后的真实路径),apply 链路 `ApplyLink` 会校验 SourceDir != "",否则报 `empty source_dir for skill X`。**当前所有产 Canonical 的入口都是**:`skilladapter.BaseAdapter.readSkillDir`(scan 出来的);`skillstore.Store.loadFromDir`(home 端 store 的 Load / LoadByName / LoadByPath 三入口共用);`conboarding post_onboarding_import.a.go`(onboarding fallback,SourcePath 透传)。**任何新增"产 Canonical"的入口必须显式写 SourceDir**,否则走 symlink 模式 apply 必炸。Apply / Undo / MigrateMode 等高层走 ApplyLink 时均依赖此不变量。
* 跨平台打开外链**统一走 `platform.platform.openExternal(url)`**(Web→window.open、Desktop→wails BrowserOpenURL),封装在 `frontend/src/platform/index.js`,不要在每个 view 重复实现;现成调用方看 SkillsView `platform.platform.openExternal(r.fallbackUrl)`、OnboardingView 等。

* `MarketAdapter` 接口加新方法时要同步更新所有实现 + 测试 stub:skillhub.go/skillssh.go/adapter_test stub(如 skillmarket_test.go 的 stubAdapter)。ListSources 返回类型从 `[]*entity.MarketSource` 改成 `[]*SourceWithHomepage` 等派生类型时,**用包装类型的 `MarshalJSON` 配合 `type alias struct { *entity; Homepage string }` 打平序列化**,前端 JSON 形状完全向后兼容(只多字段、不嵌套)。-20260704
* 首页 SkillsView 右侧详情区重构(2026-07-07):toolbar 简化成"面包屑 + 右侧操作图标",作用域区从 detail-section 下沉到 SkillFileInlinePanel 左栏顶部(以工具为父级分组,展开后竖向列出生效位置);InlinePanel 自管 scope 状态,parent 通过 `window.dispatchEvent('skillbox:scope-refresh')` 事件通知重拉。**细节记住**:文件状态同步和改动热点都在 SkillFileInlinePanel,SkillsView 的 toolbar 改名 `.detail-toolbar-name`(原来 `.detail-title-block`);移除 patchAppliedTools 后,apply/unapply 后列表项的 `applied_tools` chip 靠 store 重新拉取(不是局部 patch)。**二次重构(同日晚 2026-07-07)v2** — 顶部 detail-toolbar 整段删除(只留面包屑 + 编辑按钮的一行也删了);5 个 icon 按钮从 `.detail-actions` 搬到 `<SkillFileInlinePanel #actions>`,`[i]` 信息按钮同栏紧邻右侧。SkillsView 走 `<template #name-actions>` 传"编辑 / 取消 / 保存"按钮到 InlinePanel name 行内。
* SkillFileInlinePanel i18n + render 兜底三件套(2026-07-07):**根因** = useI18n() 在 v-if="..." 懒挂载场景下拿到 Proxy 包装的 t,computed 内调用 `t('xxx')` 抛 "t is not a function" 把整段 component update 弄崩。**v1-v2 兜底均失败**:i18n.global.t.bind + 三层 try-catch 都还会抛,因为 i18n.global.t 自身走 Proxy + onErrorCaptured 接不到 async rejection。**终版方案** = 新加 `plainT(key, values)` 暴露在 `core/i18n/index.js`,直接 `_lookup(_messages[locale][...segs])`,纯函数永不 throw。SkillFileInlinePanel 里 `const t = plainT`。其它组件(整个项目其它处)继续用 useI18n 没报这个错。**HMR 不重读 core/ 下的 export** — 加 export 后必须重启 wails dev (`pkill -f wails3 && ./run-wails` 选 1),否则用户浏览器仍报旧错。
* SkillFileInlinePanel 三个"看起来无关"的症状都是同一根因:**(1)** 作用域区一直 loading 转圈 — computed scopeGroupByTool throw → template 降级到 `v-else-if="scopeLoading"`,loading 永远显示(因为 throw 来自 computed,而非 loadScopeStatus,scopeLoading 不会被 finally 关掉);**(2)** "[i] 信息按钮第一次点击没反应,切别的 skill 才弹" — Vue 因 render 错重建整个 InlinePanel sub-tree,DOM click handler 短暂丢失;**(3)** "Unhandler error during execution of component update" — Vue 兜底提示。**诊断口诀**:看到 t is ProxyObject + 上面任一现象同时出现,先用 plainT 替换 useI18n,再排查 HMR 是否拉到新代码。

* **MD 放弃修改要点 2 次 / py 1 次就行(2026-07-08)** — SkillFileInlinePanel `resetCurrent` 跟编辑器异步 emit 撞车的经典 bug。**根因**:Tiptap `onUpdate` 是异步的(Markdown→HTML→Markdown 转换 + 防抖),用户改了一行,飞行中的 onUpdate 还没 emit 完,这时点"放弃修改" → resetCurrent 把 localFiles 重置回 orig、清 dirty → Tiptap 飞行帧落地 emit → onContentChange 再次写回用户最新内容 → dirty 重新被加上 → 看起来放弃失败,要点 2 次。Monaco 走同步 `onDidChangeContent`,没有异步窗口,所以 py 文件 1 次就行。**修法**:`resetLockUntil = Date.now() + 80` 时间窗锁,resetCurrent 加锁,80ms 内 `onContentChange` 直接 return 不更新 localFiles/dirty。**适用范围**:任何带编辑器(RichTextEditor / Monaco / CodeMirror / Ace)的"重置/放弃"场景,如果编辑器 emit 是异步 + 防抖,都得用类似时间窗锁兜底,不然会出现"放弃看起来无效"的诡异 bug。 80ms 是经验值,具体 Tiptap/Monaco 防抖时长可能不同,实际过短可调到 120-150ms。

* **CodeViewer 多分支 v-if 互斥(2026-07-08)**:CodeViewer 的 6 个渲染分支(Office / Csv / binary / md / large / cv-text-wrap)用独立 v-if + v-else 链组合时,**必须保证互斥**,否则多个兄弟元素同框渲染。CsvViewer `/ OfficeViewer / cv-md-wrap` 用独立 `v-if="isX"`(不走 v-else-if)是为了 vs 内部子组件独立 mount/unmount 干净;cv-text-wrap 紧接着用 `v-if` + 排除条件(!isMarkdown && !isOffice)切断剩下的代码/纯文本/CSV(edit 模式)走 Monaco 的路径。**特别坑的是 CSV**:csv 同时有 view 表格(`v-if="isCsv && !editable"`)和 edit Monaco,所以 cv-text-wrap 排除条件要写 `!isMarkdown && !isOffice`(不算 csv),算 csv 就会把 edit 模式 csv 卡掉 → 用户看到编辑区空白。**诊断口诀**:csv 进编辑模式白屏,先看 cv-text-wrap 排除条件有没有把 csv 误排除。

* **编辑器初始化 emit 不能当 dirty(2026-07-08)**:刚进编辑模式时,Save 按钮立刻出现的根因 —— `useEditor` 初始化(md)/ `createModel` + `setValue`(Monaco)会触发一次 onUpdate / onDidChangeContent,emit 回来的内容跟原始 orig **不完全相等**(Tiptap ProseMirror 标准化末尾换行 / 空白,Monaco createModel 会触发 onDidChangeContent),简单 `v !== orig` 判 dirty 就误报。**修法 = 内容归一化比对**:`onContentChange` 和 `isDirty` computed 都用 `normTail(s) = s.replace(/\s+$/g, '')`(只去末尾空白)比对,只要去除末尾 normalize 差异后 v 跟 orig 相等,就不标 dirty。用户真实输入会改中间任意字符,v 跟 orig 的末尾归一化后仍然不等,不影响 dirty 检测。**比"时间窗锁"更稳**:时间窗锁(MD 放弃修改那次用 80ms)依赖 emit 时机,而 emit 时机受 worker 异步加载、nextTick、watch flush 时序等影响,不一定在 80ms 内落地;归一化比对不需要猜时机。
