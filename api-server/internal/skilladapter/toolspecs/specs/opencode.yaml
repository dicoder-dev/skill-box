# OpenCode
#
# 2026-06-30 从 internal/skilladapter/opencode/opencode.go 迁来。
#
# OpenCode 本机(2026-06 探测)目录存在但为空;按标准 skill 布局写。
# 与 Cursor 同:走自己的 ~/.config/opencode/skills,不走 Agent Skills 标准。
#
# 项目级写盘 = <project>/.opencode/skills/。

tool_id: opencode
display_name: "OpenCode"
mdi_icon: "mdi:code-tags"
maturity: stable

paths:
  global:
    user:
      - "~/.config/opencode/skills"
  project:
    user:
      - ".opencode/skills"
